# 2026-08-20 工作日志

## ai-study 项目：角色 A（Agent 后端与接口）开发

### 完成内容
- 解决 Windows Defender 干预 git 嵌套 ref 的问题（详见 MEMORY.md 关键约定）
- 本地分支方案改为**单级分支 `agent-core`**（嵌套分支 `feature/agent-core` 在本地无法持久化），推送时映射为远程 `feature/agent-core`
- 首次提交 `0623aef`：FastAPI 骨架 + /api/v1/health + mock 诊断接口 + Pydantic Schema 草案
- 第二次提交 `e14429e`：核心接口闭环 + SQLite 数据层
  - contracts 补齐全部 OpenAPI 契约模型（KnowledgePoint/DiagnosisResult/MistakeRecord/ParentReport 等）
  - services/api/app/db.py：SQLite 一键初始化 + 演示学生种子数据（stu_demo_001 小明）
  - services/api/app/services/tutor.py：诊断评分→掌握度更新→路径规划→对话辅导→错题记录→复习调度→家长报告
  - 路由：diagnosis/submit、students/{id}/profile|path|mistakes、agent/chat、review/next、reports/parent
  - 9 个接口测试全部通过，服务真实启动验证通过
- 第三次提交 `8a506c8`：LangGraph 8 节点工作流 + 模型网关 + 工具层
  - services/agent/nodes.py：8 节点状态机（LangGraph StateGraph），每节点输入输出日志
  - services/agent/model_gateway.py：模型网关（MODEL_PROVIDER/LLM_PROVIDER 兼容，mock/spark/deepseek/qwen/minimax，未配 Key 回退 mock）
  - services/agent/tools.py：5 个统一工具 + 题库/知识点图谱（从 tutor.py 迁移，tutor 改为导入）
  - agent/chat 接入工作流：tool_trace 返回节点流转日志（路演证据），带 question_id 可判题
  - tests/agent/test_agent.py：10 个测试；全部 19 个测试通过
- 第四次提交 `94cd33f`：Docker 部署 + 一键启动脚本
  - docker-compose.yml、services/api/Dockerfile、.dockerignore
  - scripts/start.sh / start.bat（一键启动，已本地验证）
  - services/api/README.md 运行说明
- 第五次提交 `3aacb7e`：补交 .env.example（模型网关环境变量）
- 第六次提交 `23d6869`：生成「Agent后端与接口」代码合集文档
  - 对齐远程 main 上「角色B数学模块代码合集」格式（`# 标题 + 摘要行 + 每文件分节代码块`）
  - 包含角色 A 全部 20 个 Python 文件（1448 行）：packages/contracts、services/agent、services/api、tests
  - 注意：远程 main 上同名占位文件为空（提交 683c0e2 创建），PR 合并时需处理覆盖

### 运行方式
- 一键启动：bash scripts/start.sh（或 scripts\start.bat）
- Docker：docker compose up --build（本机未装 Docker，未本地验证）
- 测试：.venv/Scripts/python.exe -m pytest <仓库根>/tests/api <仓库根>/tests/agent -v

### 下一步（按角色文档）
- 推送到远程 feature/agent-core 并开 PR 到 develop（需 GitHub 凭据，用户操作）
- 更多学科数据接入（等 B/C/D 提供题库）
- 讯飞星火 WebSocket 真实接入（模型网关 spark 分支留 TODO）
- Docker 未本机验证（本机未安装 Docker）
