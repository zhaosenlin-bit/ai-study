# 2026-08-21 工作日志

## ai-study：同步远程 MVP 集成版

### 事件
- 远程仓库被整合为 MVP 集成版：提交 `8355f5a`（作者 Codex，2026-08-21 17:57）
  - "还原角色 A 后端(20 文件: FastAPI 8 接口 + LangGraph 8 节点 + 模型网关 + SQLite)"
  - 补齐三科数据（数学 32 节点/72 题、语文 24 节点/30 题、英语 52 节点/100 题）
  - 工具层接入 data/ 真实 JSON，前端 VITE_USE_MOCK=false 联调通过
  - 34 项测试 + 26 项全链路验证通过；文档升级 CHANGELOG v0.4
- 远程分支：main/develop/feature/agent-core 均指向 8355f5a；feature/subject-chinese=8ed7e22；feature/subject-english=d0db311

### 执行的动作
1. git fetch origin（远程跟踪 ref 再次被系统清空，用 PowerShell 重建 refs/remotes/origin/*）
2. 本地 main、develop 快进到 8355f5a
3. 本地 agent-core reset --hard 8355f5a（与远程 feature/agent-core 对齐；本地 7 个提交的成果已包含在集成版）
4. 重建 services/api/.venv（reset 时被清），安装依赖 + pytest
5. 34 项测试全部通过

### 集成版对角色 A 代码的调整（注意）
- services/agent/tools.py 被精简重构（改为读取 data/ 真实 JSON，删除内嵌 mock 题库）
- 删除了 services/api/Dockerfile、.dockerignore、README.md
- packages/contracts 新增 src/types.ts（前端 TS 类型）
- 「Agent后端与接口」代码合集被归档到 docs/contributions/
- requirements.txt：去掉 pytest，加 python-dotenv

### 本地状态
- agent-core / develop / main 三个分支均 = 8355f5a
- 跑测试前需：.venv/Scripts/python.exe -m pip install pytest

### 待办
- 后续开发在 agent-core 上继续，基于最新集成版
- 推送到远程 feature/agent-core（需 GitHub 凭据）

## ai-study：前后端联调（晚间）

### 完成
- 前端 apps/web 联调配置已在集成版就绪（client.ts 真实 API + VITE_USE_MOCK 开关 + vite proxy /api→:8000，端口 5174）
- 执行：npm install（149 包）→ 启动后端 :8000 → VITE_USE_MOCK=false 启动前端 :5174
- 验证全通：前端页面 200；代理→health/diagnosis(3 道数学题)/profile(小明)/agent/chat(8 节点流转) 均正常
- 注意：Vite 6 绑定 [::1]（IPv6），浏览器访问要用 http://localhost:5174（127.0.0.1 连不上）

### 当前后台运行
- 后端 :8000（uvicorn）、前端 :5174（vite, VITE_USE_MOCK=false）

## ai-study：登录注册系统（晚间）

### 完成
- 后端：users 表 + PBKDF2 密码哈希 + SVG 验证码（4 字符+干扰线，5 分钟过期，内存存储）+ 3 个接口
- 前端：LoginPage / RegisterPage（仿图样式：学生/家长 Tab、SVG 验证码点击刷新、表单红框校验）+ RequireAuth 守卫
- 验证码大小写不敏感（前后端均 lower() 比较，前端输入自动 lowercase）
- 端到端 6 项验证全过：注册、重复拦截、大写登录、错密码、家长注册、SVG 生成
- 预览：http://localhost:5174/register （已登录会跳主页，未登录进 /login）
